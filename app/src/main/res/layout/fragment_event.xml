<?xml version="1.0" encoding="utf-8"?>

<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="5dp"
    android:layout_marginTop="5dp">

    <TextView
        android:id="@+id/item_type"
        style="@style/SmdTheme.Events"
        android:paddingBottom="@dimen/event_header_padding"
        android:paddingTop="@dimen/event_header_padding"
        android:textAppearance="?attr/textAppearanceListItem"
        android:textColor="?attr/eventListHighlightTextColor"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/item_header"
        style="@style/SmdTheme.Events"
        android:textAppearance="?attr/textAppearanceListItem"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_type" />

    <TextView
        android:id="@+id/item_description"
        style="@style/SmdTheme.Events"
        android:textAppearance="?attr/textAppearanceListItemSecondary"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_header" />

    <TextView
        android:id="@+id/item_server"
        style="@style/SmdTheme.Events"
        android:textAppearance="?attr/textAppearanceListItemSecondary"
        android:textColor="?attr/eventListTextColor"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_description" />

    <TextView
        android:id="@+id/item_time"
        style="@style/SmdTheme.Events"
        android:layout_marginBottom="@dimen/text_margin"
        android:layout_marginTop="@dimen/text_margin"
        android:textAppearance="?attr/textAppearanceListItemSecondary"
        android:textColor="?attr/eventListHighlightTextColor"
        android:background="?attr/colorPrimaryDark"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_server" />

</android.support.constraint.ConstraintLayout>
